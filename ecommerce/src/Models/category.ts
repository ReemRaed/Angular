export default class Category {

    constructor(id: number, name: string, img: string) {
        this.id = id;
        this.name = name;
        this.img = img;
    }
    id: number;
    name: string;
    img: string;
}