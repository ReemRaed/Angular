import { ForloopDirective } from './for-loop.directive';

describe('ForloopDirective', () => {
  it('should create an instance', () => {
    const directive = new ForloopDirective();
    expect(directive).toBeTruthy();
  });
});
